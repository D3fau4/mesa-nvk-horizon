# Copyright (c) mesa-nvk-horizon contributors
# SPDX-License-Identifier: MIT
#
# No contact_links here on purpose. They need absolute URLs, which would
# hard-code one instance's hostname into the repository and break the
# moment it is mirrored or moved. The two templates point at the file
# by name instead — `SECURITY.md` — which is unambiguous and cannot rot.
blank_issues_enabled: true
